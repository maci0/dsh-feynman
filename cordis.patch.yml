- insert:
    - id: feynman-commands
      name: dsh-feynman-commands
      # Optional: rename the env refs the prompts point at. Defaults shown.
      # Secrets themselves are never config — export them or use /keys set.
      config:
        hfTokenEnv: HF_TOKEN
        alphaxivTokenEnv: ALPHAXIV_API_KEY
