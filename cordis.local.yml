# Feynman-style research workflow commands for DeepSeek Harness.
# Local-dev overlay: absolute path (per first-plugin tutorial). For the
# profile install run `dsh plugin --profile <name> add <this dir>`; the
# package declares dsh.bundle, so cordis.patch.yml ships as a layer.
- insert:
    - id: feynman
      name: '/home/maci/dsh-plugins/dsh-feynman/index.js'
      config:
        hfTokenEnv: HF_TOKEN
        alphaxivTokenEnv: ALPHAXIV_API_KEY
