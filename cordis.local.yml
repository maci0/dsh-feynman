# Feynman-style research workflow commands for DeepSeek Harness.
# Local-dev overlay: absolute path (per first-plugin tutorial). For the
# live-reload install, paste the package-name row from cordis.patch.yml
# into ~/.dsh/profiles/<profile>/cordis.patch.yml instead.
- insert:
    - id: feynman
      name: '/home/maci/Desktop/dsh-feynman/index.js'
      config:
        hfTokenEnv: HF_TOKEN
        alphaxivTokenEnv: ALPHAXIV_API_KEY
